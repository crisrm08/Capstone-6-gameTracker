<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/main.css">
    <link rel="stylesheet" href="/css/card.css">
    <title>Document</title>
</head>
<body>
    <header class="py-3 mb-3 border-bottom">
        <div class="container-fluid d-grid gap-3 align-items-center" style="grid-template-columns: 1fr 2fr;">
          <div class="logo">
            <h1>Game Tracker</h1>
            <img src="/images/controller.svg" alt="controller image">
          </div>
    
          <div class="d-flex align-items-center">
            <form class="w-100 me-3" role="search" action="/getGame" method="get">
              <input type="search" name="input" class="form-control" placeholder="Search for a game..." aria-label="Search">
            </form>
            <div class="flex-shrink-0 dropdown">
            </div>
          </div>
        </div>
      </header>

      <div class="main-content">
        <% if (games && games.length > 0) { %>
          <% games.forEach(game => { %>
            <%- include('partials/gameCard', { gameName: game.game_name, gameImage: game.game_image, rating: game.ratings, formattedDate: game.last_time_played.toISOString().split('T')[0] 
            }) %>
          <% }) %>
        <% } else  if (notFound){ %>
          <h2><%=notFound %></h2>
        <% } %>
      </div>
      
</body>
</html>